


def train_model(data):
    """Trains a simple stock prediction model."""


def predict(model, new_data):
    """Uses the trained model to predict stock prices."""
    return model.predict(new_data)
